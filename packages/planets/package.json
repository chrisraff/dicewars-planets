{
  "name": "@dicewars/planets",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build && node scripts/assert-deployable.js && npm run build:preview",
    "build:site": "vite build",
    "build:preview": "vite build --config vite.preview.config.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "@dicewars/core": "*",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "vite": "^8.2.1"
  }
}
